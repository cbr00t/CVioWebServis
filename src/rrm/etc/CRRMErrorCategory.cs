using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace CVioWebServis {
    public enum CRRMErrorCategory : int {
        Kullanici = (int)' '
        , Webci = (int)'W'
        , Programci = (int)'P'
    }
}